Run all commands in Win Powershell as Administrator (boto3,pyYAML,cfn-lint,requests)
#create a folder with name boto3_layer
#cd to boto3_layer
#pip install boto3 -t python/
#pip install pyYAML -t python/
#pip install cfn-lint -t python/
#pip install requests -t python/

Create a Layer for (portkey_ai,pydantic_core,jitter) Run all commands in Amazon Linux
# $$ Download portkey_ai packages 
#git clone https://github.com/Portkey-AI/portkey-python-sdk.git
# $$ create function.py file
#mkdir lambda-package
#cd lambda-package
#$$ Note: Place a Dockerfile and then run docker build command
#docker build -t lambda-pydantic .
#docker run --rm -v ${PWD}:/output lambda-pydantic cp /lambda-package.zip /output/
#mkdir python
#unzip lambda-package.zip -d python/
# $$ Check all the files are available in python folder or not
#zip -r lambda_layer_25_08 python
#unzip -l lambda_layer_25_08.zip : Check all the files are in zip file
#aws s3 cp lambda_layer_25_08.zip s3://prft-gen-ai-praful : Copy files in S3 bucket to create a Layer
